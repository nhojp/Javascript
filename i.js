//Create an array called colors with 3 colors. 
// Display the first color, the last color, and the total number of colors.

let colors = ["red", "blue", "green"]

console.log(colors[0]);
console.log(colors[1]);
console.log(colors.length);